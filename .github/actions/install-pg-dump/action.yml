name: Install pg_dump
description: Installs the pg_dump utility (postgresql-client-11) from the PGDG apt repo

runs:
  using: composite
  steps:
    - name: Install pg_dump
      shell: bash
      run: |
        set -euo pipefail
        sudo install -d -m 0755 /etc/apt/keyrings
        curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc \
          | sudo gpg --dearmor -o /etc/apt/keyrings/postgresql.gpg
        codename="$(lsb_release -cs)"
        echo "deb [signed-by=/etc/apt/keyrings/postgresql.gpg] http://apt.postgresql.org/pub/repos/apt/ ${codename}-pgdg main" \
          | sudo tee /etc/apt/sources.list.d/pgdg.list > /dev/null
        sudo apt-get update -qq
        sudo apt-get install -yqq postgresql-client-11
