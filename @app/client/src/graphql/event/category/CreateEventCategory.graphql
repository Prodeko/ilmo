fragment CreatedEventCategory on EventCategory {
  id
  name
  ownerOrganization {
    id
    name
  }
}

mutation CreateEventCategory($name: JSON!, $desc: JSON!, $org_id: UUID!) {
  createEventCategory(
    input: {
      eventCategory: {
        name: $name
        description: $desc
        ownerOrganizationId: $org_id
      }
    }
  ) {
    eventCategory {
      id
      ...CreatedEventCategory
    }
  }
}
