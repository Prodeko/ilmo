fragment CreatedEvent on Event {
  id
  name
  ownerOrganization {
    id
    name
  }
}

mutation CreateEvent(
  $name: String!
  $description: String!
  $organizationId: UUID!
  $categoryId: UUID!
  $startTime: Datetime!
  $endTime: Datetime!
) {
  createEvent(
    input: {
      event: {
        name: $name
        description: $description
        ownerOrganizationId: $organizationId
        categoryId: $categoryId
        startTime: $startTime
        endTime: $endTime
      }
    }
  ) {
    event {
      id
      ...CreatedEvent
    }
  }
}
