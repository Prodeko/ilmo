mutation ClaimRegistrationToken($input: ClaimRegistrationTokenInput!) {
  claimRegistrationToken(input: $input) {
    claimRegistrationTokenOutput {
      registrationToken
      updateToken
    }
  }
}
