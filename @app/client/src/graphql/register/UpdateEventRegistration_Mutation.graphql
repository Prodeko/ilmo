mutation UpdateEventRegistration(
  $updateToken: String!
  $firstName: String!
  $lastName: String!
) {
  updateRegistration(
    input: {
      updateToken: $updateToken
      firstName: $firstName
      lastName: $lastName
    }
  ) {
    registration {
      id
    }
  }
}
