fragment AdminLayout_Query on Query {
  ...SharedLayout_Query
  currentUser {
    id
    organizationMemberships(first: 20) {
      nodes {
        id
        isOwner
        organization {
          id
          name
          slug
        }
      }
    }
  }
}

query AdminLayout {
  ...AdminLayout_Query
}
