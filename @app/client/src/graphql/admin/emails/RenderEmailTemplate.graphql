query RenderEmailTemplate(
  $template: String!
  $variables: EmailTemplateVariables!
) {
  renderEmailTemplate(input: { template: $template, variables: $variables }) {
    id
    name
    html
    text
  }
}
