mutation TranslateText($from: SupportedLanguages!, $to: SupportedLanguages!, $text: String!) {
  translateText(fromLanguage: $from, toLanguage: $to, text: $text) {
    originalText
    translatedText
    __typename
  }
}